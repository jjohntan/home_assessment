1. git init

2. git checkout -b feature/assessment-answers

3. git add data_aggregation.js
git status
git commit -m "feat: add data aggregation function for completed transactions"

git add data_aggregation.js
git status
git commit -m "fix: function naming follow javascript naming convention"

git add query.sql
git status
git commit -m "feat: add sql query for users with more than 100 order total"

git add README.md
git status
git commit -m "docs: add git workflow in README"

4. git remote add origin https://github.com/jjohntan/home_assessment
git push origin feature/assessment-answers